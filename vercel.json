{
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/backend/main.py"
        }
    ],
    "builds": [
        {
            "src": "backend/main.py",
            "use": "@vercel/python"
        },
        {
            "src": "frontend/package.json",
            "use": "@vercel/static-build",
            "config": {
                "distDir": "dist"
            }
        }
    ]
}